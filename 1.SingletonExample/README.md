# Singleton Example - Implementación Básica

Este proyecto demuestra la implementación básica del **patrón Singleton** en Java. El patrón Singleton garantiza que una clase tenga una única instancia y proporciona un punto de acceso global a ella.

## 📋 Descripción

El ejemplo muestra cómo crear una clase Singleton simple con:
- Constructor privado para evitar la instanciación directa
- Método estático `getInstance()` para obtener la única instancia
- Variable estática para almacenar la instancia única
